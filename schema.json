{
  "type": "object",
  "properties": {
    "op": {
      "description": "Name of current network operator",
      "type": "string",
      "minLength": 5,
      "maxLength": 6
    },
    "ip": {
      "description": "IP address of the device as IPv4, IPv6 or hostnames",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string"
      }
    },
    "cell_id": {
      "description": "Current E-UTRAN cell ID",
      "type": "integer",
      "minimum": 0
    },
    "ue_mode": {
      "description": "UE mode of operation",
      "type": "integer",
      "minimum": 0,
      "maximum": 2
    },
    "lte_mode": {
      "description": "1 if LTE-M is supported",
      "type": "integer",
      "minimum": 0,
      "maximum": 1
    },
    "nbiot_mode": {
      "description": "1 if NB-IoT is supported",
      "type": "integer",
      "minimum": 0,
      "maximum": 1
    },
    "gps_mode": {
      "description": "1 if GNSS is supported",
      "type": "integer",
      "minimum": 0,
      "maximum": 1
    },
    "iccid": {
      "description": "SIM ICCID",
      "type": "string",
      "pattern": "^89[0-9]{17}([0-9]|F)?$"
    },
    "interval": {
      "description": "Seconds to wait before answering client",
      "type": "integer",
      "minimum": 1
    }
  },
  "required": [
    "op",
    "ip",
    "cell_id",
    "ue_mode",
    "lte_mode",
    "nbiot_mode",
    "gps_mode",
    "iccid",
    "interval"
  ],
  "additionalProperties": false
}